// @ai-generated(solo)
// Obsidian plugin: chat bubble styling for ai-memory session transcripts.
//
// All styling is done via CSS (styles.css) targeting Obsidian's native
// [!human] and [!assistant] callouts. No JS DOM manipulation needed —
// callouts render as single container divs natively.

/* eslint-disable no-undef */
const { Plugin } = require("obsidian");

module.exports = class ChatViewPlugin extends Plugin {};
