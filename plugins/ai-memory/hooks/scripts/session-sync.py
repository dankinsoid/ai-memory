#!/usr/bin/env python3
# @ai-generated(solo)
# Stop hook: no-op in file-based storage mode.
#
# Session content is persisted by the agent calling memory_session (MCP tool)
# during the session. No server sync is needed.
#
# Env-var toggles (set any to disable):
#   AI_MEMORY_DISABLED=1 — master switch (all hooks)

import sys

if __name__ == "__main__":
    sys.exit(0)
