[;; Schema: nodes (facts) + edges (associations) + tags (taxonomy).
 ;; Retrieval via tag taxonomy (ADR-009).

 ;; --- Tag Taxonomy ---
 {:db/ident       :tag/name
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one
  :db/index       true
  :db/doc         "Tag leaf name, e.g. 'clojure'"}

 {:db/ident       :tag/parent
  :db/valueType   :db.type/ref
  :db/cardinality :db.cardinality/one
  :db/doc         "Parent tag (nil = root category)"}

 {:db/ident       :tag/path
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one
  :db/unique      :db.unique/identity
  :db/doc         "Materialized path, e.g. 'languages/clojure'. This is the tag identity."}

 {:db/ident       :tag/node-count
  :db/valueType   :db.type/long
  :db/cardinality :db.cardinality/one
  :db/doc         "Materialized count of nodes with this tag. Maintained by write pipeline."}

 ;; --- Node ---
 {:db/ident       :node/id
  :db/valueType   :db.type/uuid
  :db/cardinality :db.cardinality/one
  :db/unique      :db.unique/identity
  :db/doc         "Unique node identifier"}

 {:db/ident       :node/content
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one
  :db/fulltext    true
  :db/doc         "Node content — one thought, 1-3 sentences"}

 {:db/ident       :node/type
  :db/valueType   :db.type/ref
  :db/cardinality :db.cardinality/one
  :db/doc         "Node type: fact, decision, preference, pattern, project, domain"}

 {:db/ident       :node/tag-refs
  :db/valueType   :db.type/ref
  :db/cardinality :db.cardinality/many
  :db/doc         "References to tag entities (taxonomy-aware)"}

 {:db/ident       :node/weight
  :db/valueType   :db.type/double
  :db/cardinality :db.cardinality/one
  :db/doc         "Base weight (before decay)"}

 {:db/ident       :node/cycle
  :db/valueType   :db.type/long
  :db/cardinality :db.cardinality/one
  :db/doc         "Last access/reinforcement cycle"}

 {:db/ident       :node/created-at
  :db/valueType   :db.type/instant
  :db/cardinality :db.cardinality/one
  :db/index       true
  :db/doc         "Creation timestamp (ms). Set once, never changes. Indexed for sorting."}

 {:db/ident       :node/updated-at
  :db/valueType   :db.type/instant
  :db/cardinality :db.cardinality/one
  :db/index       true
  :db/doc         "Last modification timestamp (ms). Updated on every write."}

 {:db/ident       :node/blob-dir
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one
  :db/doc         "Blob directory name under data/blobs/, e.g. '2026-02-17_blob-storage-design'"}

 {:db/ident       :node/sources
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/many
  :db/doc         "Blob source refs. Format: 'dir-name/section-file'. Multiple sources allowed."}

 {:db/ident       :node/session-id
  :db/valueType   :db.type/string
  :db/cardinality :db.cardinality/one
  :db/index       true
  :db/doc         "Claude Code session ID. Links session facts and blobs to sessions."}

 ;; --- Edge ---
 {:db/ident       :edge/id
  :db/valueType   :db.type/uuid
  :db/cardinality :db.cardinality/one
  :db/unique      :db.unique/identity
  :db/doc         "Unique edge identifier"}

 {:db/ident       :edge/from
  :db/valueType   :db.type/ref
  :db/cardinality :db.cardinality/one
  :db/doc         "Source node"}

 {:db/ident       :edge/to
  :db/valueType   :db.type/ref
  :db/cardinality :db.cardinality/one
  :db/doc         "Target node"}

 {:db/ident       :edge/weight
  :db/valueType   :db.type/double
  :db/cardinality :db.cardinality/one
  :db/doc         "Edge weight (association strength)"}

 {:db/ident       :edge/cycle
  :db/valueType   :db.type/long
  :db/cardinality :db.cardinality/one
  :db/doc         "Last reinforcement cycle"}

 ;; --- Global tick ---
 {:db/ident       :tick/value
  :db/valueType   :db.type/long
  :db/cardinality :db.cardinality/one
  :db/doc         "Global monotonic tick counter"}

 ;; --- Enums: Node Types ---
 {:db/ident :node.type/fact}
 {:db/ident :node.type/decision}
 {:db/ident :node.type/preference}
 {:db/ident :node.type/pattern}
 {:db/ident :node.type/project}
 {:db/ident :node.type/domain}
 {:db/ident :node.type/entity}
 {:db/ident :node.type/conversation}
 {:db/ident :node.type/document}
 {:db/ident :node.type/session}

]
